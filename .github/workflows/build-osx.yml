name: Build for OS X

on:
  push:
    branches: [ agent-forwarding-merge-20151128 ]
  pull_request:
    branches: [ agent-forwarding-merge-20151128 ]

jobs:
  build:
  
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: autogen
      run: ./autogen.sh
    - name: configure
      run: ./configure --prefix=/tmp/mosh-dist
    - name: make
      run: make
    - name: make install
      run: make install
    - name: zip
      run: cd /tmp/mosh-dist; zip -r ../mosh-osx.zip ./
    - uses: actions/upload-artifact@v2
      with:
        name: mosh-osx.zip
        path: /tmp/mosh-osx.zip
